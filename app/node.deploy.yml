apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-app
  namespace: jenkins-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-app
  template:
    metadata:
      labels:
        app: node-app
    spec:
      containers:
        - name: node-app-container
          image: abdo/app_image:latestt
          ports:
            - name: http-port
              containerPort: 3000
